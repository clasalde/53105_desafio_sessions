<h1>Carrito de Compras</h1>

<div class="container">
  {{#each products}}
    <div class="container">
      <p>Producto: {{this.product.title}}</p>
      <p>Cantidad: {{this.quantity}}</p>
    </div>
  {{/each}}
</div>